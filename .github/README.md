# .github

GitHub platform configuration for this repository.

- `workflows/` — GitHub Actions CI (build, contract tests, browser E2E).
- `dependabot.yml` — weekly dependency update PRs for npm packages and GitHub
  Actions.
