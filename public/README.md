# public

Static assets copied verbatim into the Astro build output (`dist/`) without
processing. Referenced from pages under the configured base path.

- `favicon.svg` — the site favicon, linked from `src/layouts/Layout.astro`.
